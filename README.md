# Portfolio (preact-davidg)

React 19 + TypeScript + Vite. Minimal Hello World with a full animation stack installed.

## Dev

```bash
pnpm install
pnpm dev          # http://localhost:5173
make quick-check  # oxlint + tsc
```

## Agents

Cursor agent config lives in `.cursor/` (rules, hooks, MCP, skills). Start at [`AGENTS.md`](./AGENTS.md).
