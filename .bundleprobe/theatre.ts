import * as THREE from 'three'
import { Canvas, useFrame } from '@react-three/fiber'
import { getProject, types } from '@theatre/core'
export const probe = { THREE, Canvas, useFrame, getProject, types }
