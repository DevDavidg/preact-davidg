import * as THREE from 'three'
import { Canvas, useFrame } from '@react-three/fiber'
import { MeshTransmissionMaterial } from '@react-three/drei'
export const probe = { THREE, Canvas, useFrame, MeshTransmissionMaterial }
